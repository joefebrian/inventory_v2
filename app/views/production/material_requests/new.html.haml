- title "New Material Request"

- content_for :breadcrumb do
  = breadcrumb [ link_to('Administrations', administrations_path), |
    link_to('Production', production_path), |
    link_to("Material Requests", production_material_requests_path), |
    "New" ] |

= hint "Fields marked with asterix are required"
#subheader
  %h3 Create new material request
  #subheader-menu
    = link_to "#{image_tag('icons/silk/arrow_left.png')} back", production_material_requests_path
= sidemenu @tab

= render :partial => 'form'
