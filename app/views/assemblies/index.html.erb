<% title "Assembling" %>

<% content_for :breadcrumb do %>
  <%= breadcrumb [ link_to("Administrations", administrations_path),
                   link_to("Assembling", assemblies_path) ] %>
<% end -%>
<% content_for :main_act do %>
  <%= link_to "#{image_tag('icons/silk/add.png')} Create new Assembling", new_assembly_path, :class => "main_link new_assemblies", :title => "Create New Assembling" %>
<% end -%>  
<%= sidemenu %>

<table class="list">
  <tr>
    <th>Type</th>
    <th>Number</th>
    <th></th>
    <th class="actions">Actions</th>
  </tr>
<% @assemblies.each do |assembly| %>
<tr>
  <td><%=h assembly.type %></td>
  <td><%=h assembly.number %></td>
  <td><%=h assembly.number %></td>
  <td class="actions">
  <%= link_to image_tag('icons/silk/application_form_edit.png'), edit_kurs_id_path(kurs_id), :class => 'new_edit_kurs_id', :title => "Edit #{kurs_id.name}" %>
  <%= link_to image_tag('icons/silk/delete.png'), kurs_id, :confirm => 'Are you sure?', :method => :delete, :title => "Delete #{kurs_id.name}" %>
  </td>
</tr>
<% end %>

</table>
