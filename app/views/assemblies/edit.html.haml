- title "Edit Assembling"

- content_for :breadcrumb do
  = breadcrumb [ link_to('Administrations', administrations_path), |
    link_to('Assembling Formula', assemblies_path), |
    link_to(@assembly.name, @assembly), |
    'Edit' ] |
  
  = sidemenu @tab
  
= hint ""

= render :partial => 'form'
