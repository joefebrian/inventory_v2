- title "New Assembling"

- content_for :breadcrumb do
  = breadcrumb [ link_to('Administrations', administrations_path), |
    link_to('Assembling', assemblies_path), |
    'Create new Assembling' ] |
  
  = sidemenu @tab
  
= hint "Fields marked with asterix are required"
#subheader
  %h3 Create new assembly formula
  #subheader-menu
    = link_to "#{image_tag('icons/silk/arrow_left.png')} back", assemblies_path

= render :partial => 'form'
