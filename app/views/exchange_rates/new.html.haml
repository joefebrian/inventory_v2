- title "New Exchange Rate"

- content_for :breadcrumb do
  = breadcrumb [ link_to('Administrations', administrations_path), |
    link_to("Exchange Rate", exchange_rates_path), |
    "Add new Exchange Rate" ] |

= render :partial => 'form'
