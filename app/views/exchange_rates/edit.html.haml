- title "Edit Exchange Rate"

- content_for :breadcrumb do
  = breadcrumb [ link_to('Administrations', administrations_path), |
    link_to("Exchange Rates", exchange_rates_path), |
    link_to("#{h @exchange_rate.currency.name}", exchange_rate_path(@exchange_rate)), |
    "Edit" ] |

= sidemenu @tab

= render :partial => 'form'

