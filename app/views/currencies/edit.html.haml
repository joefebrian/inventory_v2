- title "Edit Currency"

- content_for :breadcrumb do
  = breadcrumb [ link_to('Administrations', administrations_path), |
    link_to("Currencies", currency_path), |
    link_to("#{h @currency.code} &bull; #{h @currency.name}", currency_path(@currency)), |
    "Edit" ] |

= sidemenu @tab

= render :partial => 'form'
