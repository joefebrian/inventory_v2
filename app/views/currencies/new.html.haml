- title "New Currency"

- content_for :breadcrumb do
  = breadcrumb [ link_to('Administrations', administrations_path), |
    link_to("Currency", currencies_path), |
    "Add new Currencies" ] |

= sidemenu @tab

= render :partial => 'form'