- cache do
  - title "Edit Quotations"
  

  - content_for :breadcrumb do
    = breadcrumb [ link_to('Administrations', administrations_path), |
      link_to('Quotations', quotations_path), |
      'Create edit Quotations' ] |

  = hint "Select one of the Type"

  = render :partial => 'form'
