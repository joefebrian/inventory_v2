- title "HPP"

- content_for :breadcrumb do
  = breadcrumb [ link_to('Administrations', administrations_path), link_to('HPP', hpp_index_path) ]
  #top_right_link
    - form_for @search do |f|
      = f.text_field :name_like
      = f.submit 'search'

  = sidemenu @tab

= hint ''
#subheader
  %h3 HPP
  #subheader-menu
    = link_to "#{image_tag('icons/silk/add.png')} Generate HPP", new_hpp_path, :class => 'main_link'
