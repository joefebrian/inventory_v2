- title "New Begining Balance"

- content_for :breadcrumb do
  = breadcrumb [ link_to("Transactions", transactions_path), |
    link_to('Begining Balances', beginning_balances_path), |
    'Create new beginning balance' ]                          |

= sidemenu @tab

= hint "Select one or several categories in which the items are going to be added to the transaction"

<h3>Create Beginning Balance</h3>
= render :partial => 'form'
