<% title "Edit Project #{h @project.number}" %>
<% content_for :breadcrumb do %>
  <%= breadcrumb [ link_to("Transactions", transactions_path), link_to("Projects", projects_path), link_to(@project.number, @project), "Edit" ] %>
<% end -%>

<div id='subheader'>
  <h3>Edit Project <%=h @project.number %></h3>
</div>

<%= sidemenu %>
<%= hint "" %>

<%= render :partial => 'form' %>
