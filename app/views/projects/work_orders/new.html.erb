<% title "New Work Order" %>

<% content_for :breadcrumb do %>
  <%= breadcrumb [ link_to('Administrations', administrations_path),
    link_to('Production', production_path),
    link_to("Work Orders", production_work_orders_path),
    "New" ] %>
<% end %>

<%= hint "Fields marked with asterix are required" %>
<div id="subheader">
  <h3>Create new work order</h3>
  <div id="subheader-menu">
    <%= link_to "#{image_tag('icons/silk/arrow_left.png')} back", production_work_orders_path %>
  </div>
</div>
<%= sidemenu @tab %>

<%= render :partial => 'form' %>
