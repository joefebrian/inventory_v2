- title "New Sales Invoice"

- content_for :breadcrumb do
  = breadcrumb [ link_to('Projects', projects_path), |
    link_to(@project.number, @project), |
    link_to('Invoices', project_invoices_path(@project)), |
    'New' ] |

  = sidemenu @tab

= hint ""

= render :partial => 'form'
