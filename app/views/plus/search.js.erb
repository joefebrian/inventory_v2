<% ActiveRecord::Base.include_root_in_json = false %>
<%= @plus.to_json(:only => [:id, :code],
                  :methods => [:item_name_with_plu_code],
                  :include => { :item => { :only => [:id, :code, :name], :methods => [:name_with_code] }
                  }) %>
