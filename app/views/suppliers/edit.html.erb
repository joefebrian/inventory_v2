<% title "Edit Supplier" %>
<% content_for :breadcrumb do %>
  <%= breadcrumb [ link_to("Administrations", administrations_path),
                   link_to("Suppliers", suppliers_path),
                   link_to(@supplier.name, @supplier),
                   "Edit" ] %>
<% end -%>

<div id='subheader'>
  <h3>Edit <%=h "#{@supplier.code} :: #{@supplier.name}" %></h3>
  <div id='subheader-menu'>
    <%= link_to "#{image_tag('icons/silk/arrow_left.png')} back", suppliers_path %>
  </div>
</div>

<%= sidemenu %>
<%= hint "Both supplier code and name must be unique" %>

<%= render :partial => 'form' %>
