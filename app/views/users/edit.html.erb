<% title "Edit User" %>

<% content_for :breadcrumb do %>
  <%= breadcrumb [ link_to("Administrations", administrations_path), link_to("Users",users_path), 
    link_to(@user.username, @user), 'Edit' ] %>
<% end -%>

<%= sidemenu %>
<%= hint "username and email address must be unique" %>

<div id="subheader">
  <h3>Edit <%= @user.username %></h3>
  <div id="subheader-menu">
    <%= link_to "#{image_tag('icons/silk/arrow_left.png')} back", users_path %>
  </div>
</div>

<%= render :partial => 'form' %>
