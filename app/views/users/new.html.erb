<% title "New User" %>

<% content_for :breadcrumb do %>
  <%= breadcrumb [ link_to("Administrations", administrations_path), link_to("Users",users_path), 'New' ] %>
<% end -%>

<%= sidemenu %>
<%= hint "username must be unique" %>

<div id="subheader">
  <h3>Crate new user</h3>
  <div id="subheader-menu">
    <%= link_to "#{image_tag('icons/silk/arrow_left.png')} back", users_path %>
  </div>
</div>

<%= render :partial => 'form' %>
