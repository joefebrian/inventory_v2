- title "Edit Quotation"

- content_for :breadcrumb do
  = breadcrumb [ link_to('Transactions', transactions_path), |
    'Sales', |
    link_to('Quotation', sales_quotation_path), |
    link_to(@quotation.number, sales_quotation_path(@quotation)), |
    'Edit Quotation' ] |

  = sidemenu @tab

- content_for :main_act do
  = link_to "#{image_tag('icons/silk/page_white_text.png')} Quotation Detail", sales_quotation_path(@quotation), :class => 'main_link'

= render :partial => 'form'
