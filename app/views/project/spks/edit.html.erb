<% title "Edit SPK #{@spk.number}" %>

<div id='subheader'>
  <h3><%=h "Edit SPK # #{@spk.number}" %></h3>
  <div id='subheader-menu'>
    <%= link_to "#{image_tag('icons/silk/arrow_left.png')} back", project_spk_path(@spk) %>
  </div>
</div>

<% content_for :breadcrumb do %>
  <%= breadcrumb [ link_to("Projects", project_spks_path),
    link_to(@spk.number, project_spk_path(@spk)) ] %>
<% end -%>

<%= sidemenu %>
<%= hint "" %>

<%= render :partial => 'form' %>
