<% title "Account :: Company" -%>
<% content_for :breadcrumb do -%>
  <%= breadcrumb [ link_to('Administrations', administrations_path), link_to('Companies', companies_path) ] %>
<% end %>

<%= sidemenu @tab %>

<%= hint "Company name and subdomain must be present and unique" %>

<div id="subheader">
  <h3>Crate new company</h3>
  <div id="subheader-menu">
    <%= link_to "#{image_tag('icons/silk/arrow_left.png')} back", companies_path %>
  </div>
</div>

<%= render 'form' %>
