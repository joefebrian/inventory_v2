<% title "Edit Item" %>

<% content_for :breadcrumb do %>
  <%= breadcrumb [ link_to("Administrations", administrations_path),
                   link_to("Items",items_path),
                   'Edit Item' ] %>
<% end -%>

<%= hint "Fileds marked with asterix are required" %>
<div id="subheader">
  <h3>Edit <%= @item.name %></h3>
  <div id="subheader-menu">
    <%= link_to "#{image_tag('icons/silk/arrow_left.png')} back", item_path(@item) %>
  </div>
</div>
<%= sidemenu %>

<%= render :partial => 'form' %>
