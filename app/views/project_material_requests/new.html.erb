<% title "Project #{@project.name}" %>

<div id='subheader'>
  <h3><%= "#{h @project.number} :: Material Requests :: New" %></h3>
  <div id='subheader-menu'>
    <%= link_to "#{image_tag('icons/silk/arrow_left.png')} back", project_material_requests_path(@project) %>
  </div>
</div>

<% content_for :breadcrumb do %>
  <%= breadcrumb [ link_to("Transactions", transactions_path),
    link_to("Projects", projects_path),
    link_to(@project.number, @project),
    link_to("Material Requests", project_material_requests_path(@project)),
    "New" ] %>
<% end -%>

<%= sidemenu %>
<%= hint "" %>

<%= render :partial => 'form' %>
