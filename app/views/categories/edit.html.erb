<% title "Edit Category" %>

<% content_for :breadcrumb do %>
  <%= breadcrumb [ link_to("Administrations", administrations_path),
                   link_to("Categories",categories_path),
                   "Edit category" ] %>
<% end -%>

<%= hint "Select a parent category or leave it blank to create a parent category" %>
<div id="subheader">
  <h3>Edit <%= @category.name %></h3>
  <div id="subheader-menu">
    <%= link_to "#{image_tag('icons/silk/arrow_left.png')} back", category_path(@category) %>
  </div>
</div>

<%= sidemenu %>

<%= render :partial => 'form' %>
